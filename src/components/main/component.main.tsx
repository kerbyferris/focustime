import { SafeAreaView, Text, View } from "react-native";

const Main = () => {
  return (
    <SafeAreaView>
      <View className="items-center justify-center bg-red-500">
        <Text className="text-white">Hello hello Tailwind</Text>
      </View>
    </SafeAreaView>
  );
};

export default Main;
